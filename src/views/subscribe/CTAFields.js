import styled from '@emotion/styled';
import css from '@styled-system/css';

export const CTAFields = styled('div')(
  css({
    display: ['block', null, 'flex'],
    alignItems: 'center',
    justifyContent: ['flex-start', 'flex-end'],
    pt: [3, null, 0]
  })
);
