import React from 'react'

import Layout from '../components/layout';

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>Page not found</h1>
      <p>You once were lost, but now you are found.</p>
    </div>
  </Layout>
)

export default NotFoundPage
