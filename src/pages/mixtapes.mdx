import { Layout } from "../components/Layout";

# Mixtapes

Soul Provider is healing the world through the power of funk, soul, and software.

export default ({ children }) => <Layout title="Mixtapes">{children}</Layout>;
