{
  "name": "soulprovidr.fm",
  "description": "Soul Provider FM website.",
  "version": "1.0.0",
  "author": "Shola Anozie <shola@soulprovidr.co>",
  "dependencies": {
    "gatsby": "^1.9.277",
    "gatsby-link": "^1.6.46",
    "gatsby-plugin-react-helmet": "^2.0.11",
    "react-helmet": "^5.2.0"
  },
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "decrypt": "ansible-vault decrypt --vault-password-file .vault_pass",
    "deploy": "ansible-playbook site.yml -K --tags=website",
    "develop": "gatsby develop",
    "encrypt": "ansible-vault encrypt --vault-password-file .vault_pass",
    "format": "prettier --write 'src/**/*.js'",
    "provision": "ansible-playbook site.yml -K --tags=webserver,icecast,storage --vault-password-file .vault_pass"
  },
  "devDependencies": {
    "prettier": "^1.13.7"
  }
}
